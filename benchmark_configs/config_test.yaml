## A test config to run the benchmark

deconv_methods: ["NNLS", "MixUpVI", "MixUpVI_noise"] # "TAPE", "Scaden", "scVI", "DestVI"
evaluation_datasets: ["CTI"]
granularities: ["3rd_level_granularity"]
evaluation_pseudobulk_samplings: ["DIRICHLET"] # "UNIFORM", "PURIFIED"
n_samples_evaluation_pseudobulk: 500
n_cells_per_evaluation_pseudobulk: [256]
signature_matrices: ["CTI_3rd_level_granularity"]
train_dataset: "CTI"
n_variable_genes: 2000
experiment_name: "mixup_3rd_gran_noise_experiment" # if None, a random name will be chosen
save: True # if False, only the plots will be saved
